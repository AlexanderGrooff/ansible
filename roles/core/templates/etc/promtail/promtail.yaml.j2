# {{ ansible_managed }}

server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /var/cache/promtail/positions.yaml

clients:
- url: https://loki.alex.systems/loki/api/v1/push

scrape_configs:
- job_name: system
  static_configs:
  - targets:
      - localhost
    labels:
      job: varlogs
      __path__: /var/log/*log
